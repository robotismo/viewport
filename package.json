{
  "name": "viewport",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Viewport — an immersive space-simulation observation deck",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vite-plugin-glsl": "^1.6.0",
    "vitest": "^4.1.9"
  }
}
